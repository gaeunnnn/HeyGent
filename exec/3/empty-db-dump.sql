-- HeyGent empty database dump for submission.
-- No user data, credentials, tokens, task history, or memory records are included.
-- The application creates and updates tables through Spring JPA when it starts.

BEGIN;

CREATE EXTENSION IF NOT EXISTS vector;

COMMIT;
